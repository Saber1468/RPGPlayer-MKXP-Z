#ifndef __gl2ext_h_
#define __gl2ext_h_

/*
 * Minimal GLES2 extension header for MKXP-Z iOS builds.
 *
 * MKXP-Z loads GL functions dynamically via SDL_GL_GetProcAddress
 * so no extension prototypes are needed at compile time.
 */

#include "gl2platform.h"

#endif /* __gl2ext_h_ */
